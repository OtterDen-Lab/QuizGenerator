name: "All Question Classes Catalog"
practice: False
questions:
  1:
    # Basic Questions
    FromText:
      class: FromText
      topic: MISC # Question.Topic enum
      spacing: 0 # int

    FromGenerator:
      class: FromGenerator
      text: null # str
      generator: null # str - Python code to generate content
      possible_variations: .inf # int or float
      topic: MISC # Question.Topic enum
      spacing: 0 # int

    TrueFalse:
      class: TrueFalse
      topic: MISC # Question.Topic enum
      spacing: 0 # int

    # CST334 Math Questions
    BitsAndBytes:
      class: cst334.BitsAndBytes
      topic: MATH # Question.Topic enum (auto-set by MathQuestion)
      spacing: 0 # int

    HexAndBinary:
      class: cst334.HexAndBinary
      topic: MATH # Question.Topic enum (auto-set by MathQuestion)
      spacing: 0 # int

    AverageMemoryAccessTime:
      class: cst334.AverageMemoryAccessTime
      topic: MATH # Question.Topic enum (auto-set by MathQuestion)
      spacing: 0 # int

    # CST334 Memory Questions
    VirtualAddressParts:
      class: cst334.VirtualAddressParts
      topic: MEMORY # Question.Topic enum (auto-set by MemoryQuestion)
      spacing: 0 # int

    CachingQuestion:
      class: cst334.CachingQuestion
      num_elements: 5 # int
      cache_size: 3 # int
      num_requests: 10 # int
      policy: null # str - "FIFO", "LRU", "BELADY"
      algo: null # str - alias for policy
      topic: MEMORY # Question.Topic enum (auto-set by MemoryQuestion)
      spacing: 0 # int

    BaseAndBounds:
      class: cst334.BaseAndBounds
      topic: MEMORY # Question.Topic enum (auto-set by MemoryQuestion)
      spacing: 0 # int

    Segmentation:
      class: cst334.Segmentation
      topic: MEMORY # Question.Topic enum (auto-set by MemoryQuestion)
      spacing: 0 # int

    Paging:
      class: cst334.Paging
      topic: MEMORY # Question.Topic enum (auto-set by MemoryQuestion)
      spacing: 0 # int

    # CST334 Process Questions
    SchedulingQuestion:
      class: cst334.SchedulingQuestion
      num_jobs: 3 # int
      scheduler_kind: null # str - "FIFO", "ShortestDuration", "ShortestTimeRemaining", "RoundRobin"
      topic: PROCESS # Question.Topic enum (auto-set by ProcessQuestion)
      spacing: 0 # int

    MLFQ_Question:
      class: cst334.MLFQ_Question
      topic: PROCESS # Question.Topic enum (auto-set by ProcessQuestion)
      spacing: 0 # int
      # Note: Implementation appears incomplete

    # CST334 Language Questions
    ValidStringsInLanguageQuestion:
      class: cst334.ValidStringsInLanguageQuestion
      grammar_str_good: null # str - BNF grammar string
      grammar_str_bad: null # str - BNF grammar string
      include_spaces: false # bool
      max_length: 30 # int
      num_answer_options: 4 # int
      num_answer_blanks: 4 # int
      topic: LANGUAGES # Question.Topic enum (auto-set by LanguageQuestion)
      spacing: 0 # int

    # CST334 Persistence Questions
    HardDriveAccessTime:
      class: cst334.HardDriveAccessTime
      topic: IO # Question.Topic enum (auto-set by IOQuestion)
      spacing: 0 # int

    INodeAccesses:
      class: cst334.INodeAccesses
      topic: IO # Question.Topic enum (auto-set by IOQuestion)
      spacing: 0 # int

    VSFS_states:
      class: cst334.VSFS_states
      num_steps: 10 # int
      topic: IO # Question.Topic enum (auto-set by IOQuestion)
      spacing: 0 # int
