# LMSInterface source used for release-time vendoring.
# Keep this pinned to a stable upstream release tag.
[lms_interface]
repository = "OtterDen-Lab/LMSInterface"
ref = "v0.4.7"
expected_version = "0.4.7"
